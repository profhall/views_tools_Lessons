App Vertical
  flexShrink 1
  flexBasis auto
  justifyContent center
  flexGrow 1
  height 100vh
  Splash
    onWhen <isLoading

  Vertical
    flexBasis auto
    flexGrow 1
    flexShrink 1
    onWhen <isReady
    height 100%
    Header
      score < correct
      matchCardValue < none
      compareCardValue < none

    Cards
      onClick <cardClick
      matchValues <